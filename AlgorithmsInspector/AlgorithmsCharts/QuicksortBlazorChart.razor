<h3>Algorytm Quicksort. Rekurencyjnie vs. Iteracyjnie. Element rozdzielaj¹cy: œrodkowy</h3>
<Chart Config="_config1" @ref="_chart1"></Chart>
<br />
<h3>Algorytm Quicksort. Rekurencyjnie vs. Iteracyjnie. Element rozdzielaj¹cy: skrajny</h3>
<Chart Config="_config2" @ref="_chart2"></Chart>

@code {
    protected override async Task OnInitializedAsync()
    {
        _config1 = new LineChartConfig()
            {
                Options = new Options()
                {
                    RegisterDataLabels = true,
                    Plugins = new Plugins()
                    {
                        DataLabels = new DataLabels()
                        {
                            Align = DatalabelsAlign.Start,
                            Anchor = DatalabelsAnchor.Start,
                        }
                    }
                }
            };

        _config1.Data.Labels = LineChartLabels;
        _config1.Data.Datasets.Add(new LineDataset()
            {
                Label = "Rekurencyjnie",
                Data = LineDataRecursion_1,
                BorderColor = "red",
                Tension = 0.1M,
                Fill = true,
                PointRadius = 2,
            });
        _config1.Data.Datasets.Add(new LineDataset()
            {
                Label = "Iteracyjnie",
                Data = LineDataIteration_1,
                BorderColor = "green",
                Tension = 0.1M,
                Fill = true,
                PointRadius = 2,
            });

        _config2 = new LineChartConfig()
            {
                Options = new Options()
                {
                    RegisterDataLabels = true,
                    Plugins = new Plugins()
                    {
                        DataLabels = new DataLabels()
                        {
                            Align = DatalabelsAlign.Start,
                            Anchor = DatalabelsAnchor.Start,
                        }
                    }
                }
            };

        _config2.Data.Labels = LineChartLabels;
        _config2.Data.Datasets.Add(new LineDataset()
            {
                Label = "Rekurencyjnie",
                Data = LineDataRecursion_2,
                BorderColor = "red",
                Tension = 0.1M,
                Fill = true,
                PointRadius = 2,
            });
        _config2.Data.Datasets.Add(new LineDataset()
            {
                Label = "Iteracyjnie",
                Data = LineDataIteration_2,
                BorderColor = "green",
                Tension = 0.1M,
                Fill = true,
                PointRadius = 2,
            });
    }
}